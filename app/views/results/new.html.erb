<!DOCTYPE html>
<html>
<head>
  <title>Users</title>
</head>
<nav class="navbar navbar-default navbar-fixed-top">
  <a class="navbar-brand" href="#"><b>Fog City Run</b></a>
  <ul class="nav navbar-nav">
    <li class="nav-item">
      <a class="nav-link" <%= link_to "Home", root_path %></a>
    </li>
    <li class="nav-item">
      <a class="nav-link" <%= link_to 'Racers', racers_path %></a>
    </li>
    <li class="nav-item">
      <a class="nav-link" <%= link_to 'Races', races_path %></a>
    </li>
    <li class="nav-item">
      <a class="nav-link" <%= link_to 'Loop-Beer-Loop', loop_beer_path %></a>
    </li>
    <%= render "shared/import_button" %>
  </ul>
  <%= render "shared/login" %>
</nav>
<body style="padding-top: 60px">
<div class="container-fluid">
<h2> New Result </h2>
<%= form_for @result do |r| %>
 
  <% if @result.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@result.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @result.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>
    <%= r.label :rank %><br>
    <%= r.text_field :rank %>
  </p>
  <p>
    <%= r.label :bib %><br>
    <%= r.text_field :bib %>
  </p>
  <p>
    <%= r.label :racer_id %><br>
    <%= r.text_field :racer_id %>
  </p>
  <p>
    <%= r.label :group_name %><br>
    <%= r.text_field :group_name %>
  </p>
  <p>
    <%= r.label :time %><br>
    <%= r.text_field :time %>
  </p>
  <p>
    <%= r.label :race_id %><br>
    <%= r.text_field :race_id, :value => params[:race_id] %>
  </p>
  <p>
    <%=button_tag "Submit", class: "btn btn-default"%>
  </p>
  </div>
  <% end %>
</body>
</html>