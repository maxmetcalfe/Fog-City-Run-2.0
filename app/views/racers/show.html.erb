<!DOCTYPE html>
<html>
<head>
  <title>Racers</title>
</head>
<nav class="navbar navbar-default navbar-fixed-top">
  <a class="navbar-brand" href="#"><b>Fog City Run</b></a>
  <ul class="nav navbar-nav">
    <li class="nav-item">
      <a class="nav-link" <%= link_to 'Back', racers_path %></a>
    </li>
    <li class="nav-item">
      <a class="nav-link" <%= link_to 'Racers', racers_path %></a>
    </li>
    <li class="nav-item">
      <a class="nav-link" <%= link_to 'Races', races_path %></a>
    </li>
  </ul>
</nav>
<body style="padding-top: 40px">
<div class="container-fluid">
  <div class="row">
    <div class="col-sm-5">
      <h3> <%= @racer.first_name %> <%= @racer.last_name %></h3>
      <h4><i>Races: <%= @racer.results.count %></i></h4>
      <h4><i>PR: <%= @racer.results.order(:time).first.time%></i></h4>
    </div>
  </div>
  <table class="table table-hover table-bordered">
  <thead>
  <tr>
    <th>Date</th>
    <th>Time</th>
  </tr>
  </thead>
  <tbody>
  <% @racer.results.each do |result| %>
    <tr>
      <td><%= Race.find(result.race_id).date %></td>
      <td><%= result.time %></td>
    </tr>
  <% end %>
  </tbody>
</table>
</div>
</body>
</html>