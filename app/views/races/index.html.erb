<!DOCTYPE html>
<html>
<head>
  <title>Races</title> 
  <link href="../../assets/stylesheets/bootstrap.css" rel="stylesheet">
  <link href="../../assets/stylesheets/races.scss" rel="stylesheet">
</head>
<nav class="navbar navbar-custom navbar-fixed-top">
  <ul class="nav navbar-nav">
    <li class="nav-item">
      <%= link_to "Home", root_path, class: "btn btn-warning-navbar btn-lg-navbar" %>
    </li>
    <li class="nav-item">
      <%= link_to "Racers", racers_path, class: "btn btn-warning-navbar btn-lg-navbar" %>
    </li>
    <li class="nav-item active">
      <%= link_to "Races", racers_path, class: "btn btn-warning-navbar btn-lg-navbar" %>
    </li>
  </ul>
</nav>
<body style="padding-top: 0px">
<div class="container-fluid">
<!--   <div class="row">
    <%= column_chart Result.group(:race_id).count, min: 0, max: 22, colors: ["grey"] %>
  </div> -->
  <div class="row">
    <table class="table table-hover table-bordered">
      <thead>
      <tr>
        <th>Date</th>
        <th>Racer Count</th>
        <th>Results</th>
      </tr>
      </thead>
      <tbody>
      <% Race.order(:date).reverse.each do |race| %>
        <tr>
          <td><%= race.date %></td>
          <td><%= race.results.count %></td>
          <td><%= link_to 'Show', race_path(race) %></td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
</body>
</html>