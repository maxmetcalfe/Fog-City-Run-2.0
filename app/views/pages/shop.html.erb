<!DOCTYPE html>
<html>
<head>
  <title>Racers</title>
    <%= render "shared/navbar" %>
</head>
<body style="padding-top: 60px">
<div class="container-fluid">
<body>
  <div class="container">
    <div class="row">
      <% if @user_shop_blocked %>
      <div class="alert alert-danger">
        You need to have comleted 30 or more races before you can place an order.
      </div>
      <% elsif !@user_shop_blocked and @user_order.nil?%>
      <div class="alert alert-info">
        You have completed enough races. Please place an order.
      </div>
      <% elsif !@user_shop_blocked and !@user_order.nil? %>
        <div class="alert alert-info">
          <b>Your current order:</b>
          <%= @user_order.item %> in size <%= @user_order.size %>.
          Select a new item below to modify your selection.
        </div>
      <% end %>
      <div class="text-center">
        <div class="row">
          <b>FCR Old School T-Shirt </b>
        </div>
        <br>
        <div class="row">
        <%= image_tag "hat1.png" %>
        </div>
      </div>
      <br>
    </div>
    <% if !@user_shop_blocked %>
      <div class="row">
        <div class="text-center">
          <%= link_to 'S', new_order_path(:item => "Red Shirt", :size => "S", :user_id => current_user.id ), :class => "btn btn-default" %>
          <%= link_to 'M', new_order_path(:item => "Red Shirt", :size => "M", :user_id => current_user.id ), :class => "btn btn-default" %>
          <%= link_to 'L', new_order_path(:item => "Red Shirt", :size => "L", :user_id => current_user.id ), :class => "btn btn-default" %>
          <%= link_to 'XL', new_order_path(:item => "Red Shirt", :size => "XL", :user_id => current_user.id ), :class => "btn btn-default" %>
          <%= link_to 'XXL', new_order_path(:item => "Red Shirt", :size => "XXL", :user_id => current_user.id ), :class => "btn btn-default" %>
        </div>
      </div>
    <% end %>
    <br>
    <br>
    <div class="text-center">
      <div class="row">
        <b>FCR Trucker Hat </b>
      </div>
      <br>  
      <div class="row">
        <div class="col-md-4">
          <%= image_tag "hat1.png" %>
          <br><br>
          <% if !@user_shop_blocked %>
          <%= link_to 'One Size Fits All', new_order_path(:item => "Trucker Hat - Pink", :size => "One Size Fits All", :user_id => current_user.id ), :class => "btn btn-default" %>
          <% end %>
        </div>
        <div class="col-md-4">
          <%= image_tag "hat1.png" %>
          <br><br>
          <% if !@user_shop_blocked %>
          <%= link_to 'One Size Fits All', new_order_path(:item => "Trucker Hat - Green", :size => "One Size Fits All", :user_id => current_user.id ), :class => "btn btn-default" %>
          <% end %>
        </div>
        <div class="col-md-4">
          <%= image_tag "hat1.png" %>
          <br><br>
          <% if !@user_shop_blocked %>
          <%= link_to 'One Size Fits All', new_order_path(:item => "Trucker Hat - Blue", :size => "One Size Fits All", :user_id => current_user.id ), :class => "btn btn-default" %>
          <% end %>
        </div>
      </div>
    </div>
    <br>
  </div>
</div>
</body>
</html>
