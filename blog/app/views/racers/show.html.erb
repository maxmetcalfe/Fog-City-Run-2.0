<!DOCTYPE html>
<html>
<head>
  <title>Racers</title>
</head>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <a class="navbar-brand" href="#"><%= @racer.first_name %> <%= @racer.last_name %> (<%= @racer.results.count %>) - <%= @racer.results.order(:time).first.time%></a>
  <ul class="nav navbar-nav">
    <li class="nav-item">
      <a class="nav-link" <%= link_to 'Racers', racers_path %></a>
    </li>
    <li class="nav-item">
      <a class="nav-link" <%= link_to 'Races', races_path %></a>
    </li>
  </ul>
</nav>
<body style="padding-top: 60px">
<table class="table table-hover">
  <thead>
  <tr>
    <th>Date</th>
    <th>Time</th>
  </tr>
  </thead>
  <tbody>
  <% @racer.results.each do |result| %>
    <tr>
      <td><%= Race.find(result.race_id).date %></td>
      <td><%= result.time %></td>
    </tr>
  <% end %>
  </tbody>
</table>
</body>
</html>