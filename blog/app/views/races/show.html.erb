<!DOCTYPE html>
<html>
<head>
  <title>Racers</title>
</head>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <ul class="nav navbar-nav">
    <li class="nav-item">
      <a class="nav-link" <%= link_to 'Back', races_path %></a>
    </li>
    <li class="nav-item">
      <a class="nav-link" <%= link_to 'Racers', racers_path %></a>
    </li>
    <li class="nav-item">
      <a class="nav-link" <%= link_to 'Races', races_path %></a>
    </li>
  </ul>
</nav>
<body style="padding-top: 60px">
<table class="table table-hover">
  <thead>
  <tr>
    <th>Rank</th>
    <th>Bib</th>
    <th>Racer</th>
    <th>Racer Results</th>
    <th>Time</th>
  </tr>
  </thead>
  <tbody>
  <% @race.results.order(:rank).each do |result| %>
    <tr>
      <td><%= result.rank %></td>
      <td><%= result.bib %></td>
      <td><%= result.racer.first_name %>  <%= result.racer.last_name %></td>
      <td><%= link_to 'Link', racer_path(result.racer) %></td>
      <td><%= result.time %></td>
    </tr>
  <% end %>
  </tbody>
</table>
</body>
</html>